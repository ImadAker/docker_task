CREATE DATABASE stats;
use stats;

CREATE TABLE cpu_percentage (
  percentage VARCHAR(20),
  time VARCHAR(100)
);

CREATE TABLE mem_percentage (
  percentage VARCHAR(20),
  time VARCHAR(100)
);

CREATE TABLE disk_percentage (
  percentage VARCHAR(20),
  time VARCHAR(100)
);

